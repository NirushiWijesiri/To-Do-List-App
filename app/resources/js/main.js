// User click on the add button
// If there is any text inside the field, add it to the todo list
document.getElementById('add').addEventListener('click', function(){
    var value = document.getElementById('item').value;
    if(value){
        
    }
});